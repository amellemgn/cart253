class Character extends Planet{
  constructor (x, y, array, landState, eatSound){
    super(x, y, array, landState, eatSound) // can i use these although they're titled diferently in the planet file??
  }

  move(){ // do i add the others, from the parent class to the js file

  }
}
